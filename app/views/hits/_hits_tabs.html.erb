<%= bootstrap_flavour_tabs(
      {
        'Summary'    => summary_site_hits_path(@site),
        'All'        => site_hits_path(@site),
        'Errors'     => errors_site_hits_path(@site),
        'Archives'   => archives_site_hits_path(@site),
        'Redirects'  => redirects_site_hits_path(@site),
        'Other'      => other_site_hits_path(@site)
      }, active: active
    )
%>
