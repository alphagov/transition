<%= render partial: 'hits',
  locals: {
     hits: @category.hits,
     site: @site,
     point_categories: [@category],
     title: "#{@category.title}",
     active: "#{@category.title}",
     no_content_message: no_content_message_for(@period, site: @site, category: @category, filtered: @filtered)
   }
%>
