<%= render partial: 'hits',
  locals: {
     hits: @category.hits,
     site: @site,
     points: @category.points,
     title: "#{@site.abbr} #{@category.title}",
     active: "#{@category.title}",
     no_content_message: "There are no #{@category} for #{@site.abbr} yet."
   }
%>
