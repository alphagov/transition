<%= render partial: 'hits',
  locals: {
     hits: @hits,
     site: @site,
     points: @points,
     title: "#{@site.abbr} Other",
     active: 'Other',
     no_content_message: "There are no other hits for #{@site.abbr} yet."
   }
%>
