<%= render partial: 'hits',
  locals: {
     hits: @category.hits,
     site: @site,
     points: @category.points,
     title: "All hits",
     active: 'All hits',
     no_content_message: no_content_message_for(@period, site: @site, period: @period, filtered: @filtered)
   }
%>
