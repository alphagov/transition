<%= render partial: 'hits',
  locals: {
     hits: @hits,
     site: @site,
     title: "#{@site.abbr} Hits",
     active: 'All',
     no_content_message: "We don’t have any traffic data for #{@site.abbr} yet."
   }
%>
