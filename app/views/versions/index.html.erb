<% content_for :page_title, 'Versions' %>

<%= breadcrumb(@versions.last) %>

<%= render partial: 'mappings/versions', locals: { detailed: true } %>

<%= javascript_tag do %>
  GOVUK.Versions.ready();
<% end %>
