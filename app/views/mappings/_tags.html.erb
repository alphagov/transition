<% if mapping.tags.any? %>
  <span class="rm">Tagged with:</span>
  <ul class="tag-list remove-bottom-margin pull-right">
    <% mapping.tag_list.each do |tag| %>
      <li>
        <%= link_to tag, filter_by_tag_path(tag), class: 'tag' %>
      </li>
    <% end %>
  </ul>
<% end %>
