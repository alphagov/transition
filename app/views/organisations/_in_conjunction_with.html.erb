<% if @organisation.parent.present? && @organisation.whitehall_type.present? %>
  <p class="subtitle">is <%= relationship_display_name(@organisation.whitehall_type) %> of
    <%= link_to @organisation.parent.title, organisation_path(@organisation.parent) %>
  </p>
<% end %>
