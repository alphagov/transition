<% content_for :page_title, 'Organisations' %>

<h1>Organisations</h1>

<table class="organisations table table-bordered">
  <thead>
  <tr>
    <th>Organisation</th>
    <th>Launch date</th>
  </tr>
  </thead>

  <tbody>
  <% @organisations.each do |org| %>
      <tr>
        <td>
          <%= link_to org.title, organisation_path(org) %>
        </td>
        <td>
          <%= org.launch_date.to_s(:long_ordinal) %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
