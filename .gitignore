# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile ~/.gitignore_global

# Ignore bundler config
/.bundle

# Ignore everything in the data directory...
/data/*
# ...except this file, so that we keep the (otherwise empty) data directory itself
!/data/.gitkeep

# Ignore the default SQLite database.
/db/*.sqlite3

# Ignore all logfiles and tempfiles.
/log/*.log
/tmp
/coverage

# Ignore structure.sql (since we've returned to schema.rb)
# in case anyone runs db:schema:dump and checks in the result
/db/structure.sql

# Ignore secret things
/config/ga_*

# Ignore spec/examples.txt for re-running
/spec/examples.txt

# Ignore JavaScript dependencies
/node_modules

# Ignore compiled assets
/public/assets
/app/assets/builds/*
!/app/assets/builds/.keep

# https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
